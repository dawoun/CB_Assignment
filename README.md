To build, use `./build.sh` (uses g++)

To run, first run the server with the pipename:

`$ ./server <pipename> &`

then run the client with number of entries + pipename:

`$ ./client <amount> <pipename>`


Good luck!
